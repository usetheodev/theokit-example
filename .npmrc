# Canonical 2026-06-16: theokit OSS framework has transitional peer-dep conflict
# between @theokit/sdk@1.x (zod@^4) and theokit@0.2.x (zod@^3). Until the
# theokit packages align on a single zod major, use legacy-peer-deps for
# npm ci to succeed without --force.
legacy-peer-deps=true
